#!/usr/bin/env bash

php -i
php --version
composer --version
echo "Node:"
node --version
echo "NPM:"
npm --version
echo "Yarn:"
yarn --version
drush --version
wp --version --allow-root
git --version
fab --version
mkdocs --version